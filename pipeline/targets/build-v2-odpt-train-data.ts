/**
 * Target list for batch v2 ODPT Train DataBundle builds.
 *
 * Each entry is a source-name returned by `--list`.
 *
 * Comment out entries to temporarily skip them.
 */
export default ['yurikamome'];
