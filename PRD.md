# プロダクト要件定義書 (PRD)

Athenai Transit

## 1. コンセプト

### 名称の由来

**「Athenai (アテナイ)」**は、「宛てない(目的地を持たない)旅」というコア体験と、古代ギリシャの学問と探索の都「アテナイ」を掛け合わせた名称。

### エレベーターピッチ

Athenai は、フリー乗車券を持った旅行者や、休日にふらっと街歩きを楽しみたい人のためのWebアプリ。
既存の乗り換え案内アプリが「目的地までの最短ルート」を提示するのに対し、Athenaiは「現在地周辺から次に出発する便」だけを地図とリストで直感的に提示する。ユーザーは画面端のアイコン(エッジマーカー)を頼りに未知のバス停へと歩き出し、直近の出発便を比較検討しながら、思いつくままに次の行き先を決定できる。

### コアバリュー

- **目的地は無い**: あえて目的地入力機能を排除し、「現在地周辺の選択肢」に100%フォーカスする。
- **直感的な地図UI**: 街歩きに最適な詳細な地形や路地がわかる地理院地図をベースマップに採用し、現在地を中心に周辺の交通機関を可視化。
- **歩く方向を指し示す「エッジマーカー」**: 現在の地図表示領域外にあるバス停や駅の方向を、画面の端にアイコンとして表示。コンパスのように「あっちへ歩けば乗り物がある」という探索のワクワク感を生み出す。
- **比較検討を楽しむ「直近n便」のボトムシート**: 各路線の次発便は「あと〇分」で強調表示し、次々発以降は絶対時刻(例: 14:30)で表示。飛び乗るか、もう少し散歩を続けるかの意思決定をサポート。
- **どこでも使える「仮想統合発着案内」**: 空港や大規模ターミナルの発着案内ボードを、事業者の垣根を越えて仮想的に再現する。本来そうした統合ボードはターミナルにしか存在しないが、Athenai Transit は現在地周辺ののりばを集約し、住宅街でも、どこでも「自分専用の発車案内」を提供する。

### ユーザー体験のイメージ

はじめて訪れた街を散歩していて「次はどこへ行こうか」と思ったとき。

Athenaiを開くと、地図の端にひっそりと小さなバスのアイコンが表示されている。そのマーカーを頼りに路地を歩いていくと、やがて地図上にバス停のピンが現れる。画面下部のリストを引き上げると、「あと5分で出発する、見知らぬ地名行きのバス」があることに気づき、ふらっとそれに乗り込んで新しい街へ向かう。そんな偶然の連続を楽しむことができる。

## 2. 対象ユーザーとデバイス

- **対象ユーザー**: フリー乗車券を持った旅行者、休日にふらっと街歩きを楽しみたい人
- **対象デバイス**: スマートフォン(モバイルブラウザ環境を最優先)、タブレット、PC
- **バックエンド**: なし。全てのデータ処理はブラウザ内で完結。

## 3. 機能要件 (UI/UX)

### A. 表示・動作の制御

App の表示と動作は、ユーザーが地図上のコントロールパネルで切り替えられる3つの軸で制御する。情報レベル (表示する情報量)、動作モード (取得するデータ量)、描画モード (停留所マーカーの描画方式) の3つである。

#### 情報レベル (InfoLevel)

UI の情報量を4段階で制御する。各レベルは閾値方式で動作し、上位レベルは下位レベルの表示内容を全て含む。

| レベル       | 原則                                                                                                                                                                                                                |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **simple**   | ユーザーにとって必須の情報のみ。行先名、路線バッジ、終点/乗車不可など、次の行動判断に不可欠な要素だけを表示する。翻訳名や補足情報は省略するが、安全や利用可否に関わる警告は必ず含める。画面を最大限シンプルに保つ。 |
| **normal**   | 日常利用向けのデフォルト。simple の必須情報に加え、翻訳名、路線の正式名称、運行頻度など、利便性を高める補足情報を追加する。                                                                                         |
| **detailed** | より深い理解を求めるユーザー向け。事業者バッジ、路線一覧、乗車地表示など、運行の全体像を把握するための情報を追加する。                                                                                              |
| **verbose**  | 開発・デバッグ用。内部 ID (stop_id, route_id, agency_id)、データ dump、メタデータなど、データ構造を直接確認するための情報を全て表示する。一般ユーザー向けではない。                                                 |

#### 動作モード (PerfMode)

取得するデータの範囲・件数を3段階で切り替え、端末性能や探索範囲に応じて性能と網羅性のバランスを選べる。

| モード     | 原則                                                                   |
| ---------- | ---------------------------------------------------------------------- |
| **lite**   | 取得する範囲・件数を絞り、低スペック端末や広域の概観でも軽快に動かす。 |
| **normal** | 日常利用向けのデフォルト。標準的な範囲・件数で表示する。               |
| **full**   | 取得する範囲・件数を広げ、周辺をより網羅的に把握する。                 |

#### 描画モード (RenderMode)

停留所マーカーの描画方式を切り替える。

| モード          | 原則                                             |
| --------------- | ------------------------------------------------ |
| **auto**        | 表示状況に応じて標準・軽量を自動で選ぶ。         |
| **standard**    | リッチな描画。枠線や選択強調など視覚表現が豊か。 |
| **lightweight** | 軽量な描画。マーカーが多くても軽快に保つ。       |

動作モード・描画モードはセッション内の一時設定であり、永続化しない (L 参照)。情報レベルは永続化する (L 参照)。

### B. 画面サイズに応じたレイアウト

- **モバイル (狭い画面) 用 UI**: 地図を主表示とし、下部に引き上げ可能な StopTimes の BottomSheet を重ねる。探索の主導権は常に地図側にあり BottomSheet は必要に応じて引き上げて確認する。
- **デスクトップ (広い画面) 用 UI**: MultiPane レイアウトを採用し、Map、StopTimes、必要に応じて補助パネルを横並びまたは分割表示する。各ペインは同じ選択・フィルター・日時の状態を共有し、片方の操作結果が他方へ即時反映される。
- **レスポンシブ切り替え**: 画面サイズに応じてレイアウトを自動的に切り替える。狭い画面では モバイル用UI を適用し、広い画面では デスクトップ用UI を適用する。
- **体験の一貫性**: モバイルとデスクトップで UI 形状は異なっても、停留所選択、フィルター、日時変更、現在地追従などの主要機能は同じ意味と結果を持つこと。
- **モバイルファースト**: 対象デバイスは引き続きスマートフォン最優先とするが、広い画面では情報比較や複数ペイン閲覧に適したデスクトップ向けレイアウトを提供する。

### C. 地図表示機能 (Map view)

- **ベースマップ**: 地理院地図(標準地図)など複数の地図から任意の地図を選択可能とする。
- **現在地表示と追従**: Geolocation APIを使用し、ユーザーの現在地を地図上に表示。現在地追従のON/OFF切り替えを可能にする。
- **停留所/駅ピンの表示**: 画面の表示領域(Bounding Box)内にある停留所/駅をアイコン(ピン)で表示する。バスと鉄道は視覚的に区別する。
- **停留所と路線のハイライト**: 停留所が選択された場合、その停留所と、関連する路線、路線上の停留所全てをハイライト表示する。これにより、路線図データが無い路線(京王バスなど)でも路線走行区間を可視化できる。

### D. エッジマーカー (Map view 画面外インジケーター) 機能

- 現在の地図表示領域の「外側(例: 画面端から半径1~2km以内)」にある停留所/駅の方向を、画面の四隅や端にアイコンとして張り付けて表示する。
- ユーザーが「この方向に歩けば乗り物がある」と直感できるようにする。
- 地図のスクロール(パン)に連動して、リアルタイムに位置と表示を更新する。

### E. 日時セレクター

- ユーザーが任意の日時を指定して、過去や未来の乗換案内を探索できる。
- ヘッダーの日時表示をタップすると日時セレクターが開き、カスタム日時を設定可能。
- カスタム日時モード中はヘッダーにインジケーターが表示され、現在時刻に戻すボタンが提供される。
- `?time=` URL パラメータでも初期日時を指定可能 (デモ URL の共有に使用)。
- 日時変更に応じて、出発案内リスト、時刻表、停留所の統計情報が動的に更新される。

### F. Portal / Anchor (お気に入り Stop)

- **Anchor**: ユーザーが明示的に登録するお気に入り Stop。NearbyStop カードのトグルボタンで追加/解除する。
- **Portal**: 登録済み Anchor の一覧ドロップダウン。画面上部に履歴と並んで表示され、選択すると地図がその Stop にパンする。
- **データモデル**: 停留所への軽量参照 (ID・名称・座標・路線種別など) として保持し、GTFS データ更新に強い設計とする。
- **グルーピング**: Anchor を名前付きグループに分類できる (将来拡張)。
- **永続化**: localStorage に永続化する (将来的な Web API への移行も見据える)。

### G. 運行情報 (StopTimes) 機能

- 同じ周辺停留所データに対して、複数の View を提供する。各 View は並び順やグルーピング単位が異なり、ユーザーは目的に応じて表示方法を切り替えられる。

#### 例) `Stop` view

- 地図の中心、または現在地から最も近い停留所の情報を優先して表示する。
- **表示件数の制限**: 各路線/行き先(系統)ごとに「直近の最大n便」のみを表示する。
- **時刻のUI表現**:
    - 1便目(次発): 現在時刻から計算し、「あと〇分」という相対時間で強調表示する。
    - 2便目/3便目: 「14:30」などの絶対時刻でシンプルに表示し、比較検討の材料とする。
- 経由地などの情報があれば併記し、行き先の想像を膨らませる工夫を入れる。

#### 例) `Route-Headsign` view

- 1つの停留所内で、出発案内を Route+Headsign 単位にグルーピングして表示する。
- 同一グループ内では直近の便を時系列順に表示し、ユーザーが同じ行先・同じ系統の次発候補をまとめて比較できるようにする。
- **表示件数の制限**: 各 Route+Headsign グループごとに「直近の最大n便」のみを表示する。
- グループ見出しでは、路線や行先がひと目で区別できることを優先し、必要に応じて路線バッジや行先を表示する。
- 個々の便を選択したときは、その Route+Headsign に対応する時刻表詳細へ遷移または展開できること。

### H. 時刻表 (Timetable)

- 停留所を起点として、より詳細な時刻表を開けること。
- 時刻表では、出発案内リストより広い時間範囲を一覧でき、便の全体像を把握できること。
- 日時セレクターや関連フィルターの状態と連動し、探索中の日時・条件に対応した内容を表示すること。

### I. 行程 (TripInfo)

- 個々の便に対して、路線、行先、事業者、乗降可否などの行程情報を簡潔に表示すること。
- 出発案内リスト、時刻表、TripInfo 表示など複数の文脈で再利用できる共通表現を持つこと。
- ユーザーが「どの便に乗るか」を比較判断するための最小限の識別情報を常に提供すること。

### J. 発着案内

- 現在地周辺の複数ののりばを集約し、空港や大規模ターミナルの発着案内ボード(発車標)のように、直近の発着便を一覧表示すること。
- 現実には事業者ごとに分かれて存在しない統合発着案内を、事業者の垣根を越えて仮想的に構成する。これにより、ターミナルでなくてもどこでも周辺の発着便をまとめて把握できること。
- 集約する範囲(半径)をユーザーが切り替えられ、選択中の範囲を地図上にも示すこと。
- 交通機関の種別に応じて見せ方を変える(バス等は複数路線を1つのボードにまとめ、鉄道等は路線・方向ごとに分ける)こと。
- 出発便・到着便の表示はそれぞれ切り替えられること。
- 時刻表現は運行情報 (G) と同様に、次発は相対時間、それ以降は絶対時刻で示すこと。

### K. 選択インタラクション

停留所選択は「どの停留所が選択されているか」という状態であり、複数の方法でトリガーされる。

停留所選択をトリガーする方法 (例):

- 地図上の停留所マーカーをクリック
- 発着案内の停留所をクリック
- 履歴から選択
- Anchor / Portal から選択
- URL パラメータ `?stop=` による初期選択

| トリガー                         | 選択状態の変化 | 視覚効果                                                                                     |
| -------------------------------- | -------------- | -------------------------------------------------------------------------------------------- |
| 停留所が選択された               | stop 選択 ON   | 選択した停留所のマーカーとその停留所に関連する路線、路線上の停留所全てをハイライト表示する。 |
| 選択中の停留所を再選択           | 変化なし       | -                                                                                            |
| 選択を解除 (地図背景クリック 等) | stop 選択解除  | ハイライトを解除し、デフォルト表示に戻す。                                                   |

### L. Web Storage による永続化

Web storage が使える環境では、以下の情報をセッション間で永続化する。

- **ユーザー設定**: 表示言語、テーマ、地図タイル、情報レベル など
- **Stop 選択履歴**: 直近に選択した Stop の一覧
- **Anchor**: お気に入り Stop の一覧 (F 参照)
- **有効化データソース**: ロード対象として選択中のデータソースの一覧

**利用不可環境での動作**: ブラウザ設定 (Chrome の「全 Cookie とサイトデータをブロック」等) やプライベートモードで Web storage が使えない場合でも、crash せず起動し、起動時に 1 度だけ「永続化されない」旨を通知する。永続化前提の操作 UI (アンカー・履歴など) は機能発見性のため表示を維持し、操作の失敗は通常どおり通知する。

### M. 多言語対応

- 表示言語をユーザーが切り替えられること。
- データに含まれる多言語表記 (翻訳名など) を、選択中の言語に応じて表示すること。
- 初期言語は `?lang=` (BCP 47) で指定でき、サポート外の値はデフォルト言語にフォールバックすること。
- 選択した言語は永続化する (L 参照)。

### N. データソース選択

- ユーザーが利用するデータソース (事業者/路線群) を選択して読み込めること。
- 多数のデータソースに対応し、選択したものだけをロード対象とすること。
- 選択状態は永続化し (L 参照)、`?sources=` で一時的に上書きできること (永続化された設定自体は変更しない)。

## 4. システムアーキテクチャ

本プロジェクトは、APIサーバーを持たない完全フロントエンド完結のアーキテクチャを採用する。

- **データパイプライン**: Node.js の事前ビルドパイプラインにより、GTFS/ODPT オープンデータから最適化済み JSON を生成し、静的ファイルとして配信する。
- **リポジトリパターン**: UI コンポーネントは Repository インターフェースにのみ依存し、データソースと疎結合を保つ。

## 5. URL パラメータ

URL パラメータでアプリの動作を制御できる。パラメータは組み合わせて使用可能。

| パラメータ            | 説明                                   | 例                             |
| --------------------- | -------------------------------------- | ------------------------------ |
| `?sources=<prefixes>` | 指定した prefix のデータソースのみ有効 | `/?sources=minkuru,yurimo`     |
| `?sources=all`        | disabled を含む全データソースを有効    | `/?sources=all`                |
| `?lat=<number>`       | 初期表示の緯度 (-90 〜 90)             | `/?lat=35.68&lng=139.77`       |
| `?lng=<number>`       | 初期表示の経度 (-180 〜 180)           | `/?lat=35.68&lng=139.77`       |
| `?zm=<number>`        | 初期表示のズームレベル (1 〜 20)       | `/?lat=35.68&lng=139.77&zm=15` |
| `?time=<ISO8601>`     | 初期日時の指定 (RFC 3339)              | `/?time=2026-03-25T20:55`      |
| `?stop=<stop_id>`     | 初期選択 stop の指定                   | `/?stop=minkuru:0442-01`       |
| `?tileIdx=<number>`   | タイルソースの初期選択 (0-based index) | `/?tileIdx=3`                  |
| `?lang=<code>`        | 表示言語の初期選択 (BCP 47)            | `/?lang=en`                    |

## 6. 将来構想

- **GTFS-Flex対応**: 停留所(点)だけでなく、オンデマンド交通の乗降可能エリア(面/ポリゴン)を地図上にオーバーレイ表示し、探索の幅を広げる。
- **GTFS-Realtime対応**: リアルタイムの遅延情報を加味した、より正確な出発案内の実現。

## 7. 関連文書

本書は要件 (何を・なぜ) を定義する。より詳細な仕様や実装に関する情報は以下の文書に定義する。

- [README.md](./README.md) -- プロジェクト概要
- [DEVELOPMENT.md](./DEVELOPMENT.md) -- 開発者向け入口 (コード品質・ファイル配置・アプリ構成)
- [CLAUDE.md](./CLAUDE.md) -- アーキテクチャ概要と開発ルール
- [docs/README.md](./docs/README.md) -- 詳細実装ドキュメントの索引 (地図・ストレージ・ランタイム設定など)
- [pipeline/README.md](./pipeline/README.md) -- データパイプライン (独立 subsystem)
