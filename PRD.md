# プロダクト要件定義書 (PRD): Athenai

## 1. コンセプト

### 名称の由来

**「Athenai(アテナイ)」**は、「宛てない(目的地を持たない)旅」というコア体験と、古代ギリシャの学問と探索の都「アテナイ」を掛け合わせた名称。

### エレベーターピッチ

Athenaiは、フリー乗車券を持った旅行者や、休日にふらっと街歩きを楽しみたい人のためのWebアプリ。
既存の乗り換え案内アプリが「目的地までの最短ルート」を提示するのに対し、Athenaiは「現在地周辺から次に出発する便」だけを地図とリストで直感的に提示する。ユーザーは画面端のアイコン(エッジマーカー)を頼りに未知のバス停へと歩き出し、直近の出発便を比較検討しながら、思いつくままに次の行き先を決定できる。

### コアバリュー

- **目的地は無い**: あえて目的地入力機能を排除し、「現在地周辺の選択肢」に100%フォーカスする。
- **歩く方向を指し示す「エッジマーカー」**: 現在の地図表示領域外にあるバス停や駅の方向を、画面の端にアイコンとして表示。コンパスのように「あっちへ歩けば乗り物がある」という探索のワクワク感を生み出す。
- **直感的な地図UI(地理院地図 x Leaflet)**: 街歩きに最適な詳細な地形や路地がわかる地理院地図をベースマップに採用し、現在地を中心に周辺の交通機関を可視化。
- **比較検討を楽しむ「直近n便」のボトムシート**: 各路線の次発便は「あと〇分」で強調表示し、次々発以降は絶対時刻(例: 14:30)で表示。飛び乗るか、もう少し散歩を続けるかの意思決定をサポート。

### ユーザー体験のイメージ

例えば、中野の街を散歩していて「次はどこへ行こうか」と思ったとき。

Athenaiを開くと、地図の端にひっそりと小さなバスのアイコンが表示されている。そのマーカーを頼りに路地を歩いていくと、やがて地図上にバス停のピンが現れる。画面下部のリストを引き上げると、「あと5分で出発する、見知らぬ地名行きのバス」があることに気づき、ふらっとそれに乗り込んで新しい街へ向かう。そんな偶然の連続を楽しむことができる。

## 2. 対象ユーザーとデバイス

- **対象ユーザー**: フリー乗車券を持った旅行者、休日にふらっと街歩きを楽しみたい人
- **対象デバイス**: スマートフォン(モバイルブラウザ環境を最優先)
- **バックエンド**: なし。全てのデータ処理はブラウザ内で完結。

## 3. 機能要件 (UI/UX)

### A. 情報レベル (InfoLevel)

UI の情報量を4段階で制御する。ユーザーが地図上のコントロールパネルで切り替える。各レベルは閾値方式で動作し、上位レベルは下位レベルの表示内容を全て含む。

| レベル       | 原則                                                                                                                                                                                                                |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **simple**   | ユーザーにとって必須の情報のみ。行先名、路線バッジ、終点/乗車不可など、次の行動判断に不可欠な要素だけを表示する。翻訳名や補足情報は省略するが、安全や利用可否に関わる警告は必ず含める。画面を最大限シンプルに保つ。 |
| **normal**   | 日常利用向けのデフォルト。simple の必須情報に加え、翻訳名 (subNames)、路線の正式名称、運行頻度など、利便性を高める補足情報を追加する。                                                                              |
| **detailed** | より深い理解を求めるユーザー向け。事業者バッジ、路線一覧、乗車地表示など、運行の全体像を把握するための情報を追加する。                                                                                              |
| **verbose**  | 開発・デバッグ用。内部 ID (stop_id, route_id, agency_id)、データ dump、メタデータなど、データ構造を直接確認するための情報を全て表示する。一般ユーザー向けではない。                                                 |

### B. 画面サイズに応じたレイアウト

- **モバイル (狭い画面) 用 UI**: 地図を主表示とし、下部に引き上げ可能な StopTimes の BottomSheet を重ねる。探索の主導権は常に地図側にあり BottomSheet は必要に応じて引き上げて確認する。
- **デスクトップ (広い画面) 用 UI**: MultiPane レイアウトを採用し、Map、StopTimes、必要に応じて補助パネルを横並びまたは分割表示する。各ペインは同じ selection / filter / time state を共有し、片方の操作結果が他方へ即時反映される。
- **レスポンシブ切り替え**: 画面サイズに応じてレイアウトを自動的に切り替える。狭い画面では モバイル用UI を適用し、広い画面では デスクトップ用UI を適用する。
- **体験の一貫性**: モバイルとデスクトップで UI 形状は異なっても、停留所選択、路線選択、フィルター、日時変更、現在地追従などの主要機能は同じ意味と結果を持つこと。
- **モバイルファースト**: 対象デバイスは引き続きスマートフォン最優先とするが、広い画面では情報比較や複数ペイン閲覧に適したデスクトップ向けレイアウトを提供する。

### C. 地図表示機能 (Map view)

- **ベースマップ**: 地理院地図(標準地図)を使用し、街歩きに適した詳細な地形や道を表示する。
- **現在地表示と追従**: Geolocation APIを使用し、ユーザーの現在地を地図上に表示。現在地追従のON/OFF切り替えを可能にする。
- **停留所/駅ピンの表示**: 画面の表示領域(Bounding Box)内にあるGTFSデータの停留所/駅をアイコン(ピン)で表示する。バスと鉄道は視覚的に区別する。
- **選択路線の停留所表示 (Route Stops Layer)**: 路線を選択した場合、その路線上の全停留所を専用レイヤーで表示。これにより、路線図データが無い路線(京王バスなど)でも路線走行区間を可視化できる。Route Stops Layer は nearby/far layer より描画優先度が高く、重複する停留所は Route Stops Layer のみに表示される。

地図レイヤーの z-index 割り当てと rendering priority については [docs/map-architecture.md](./docs/map-architecture.md) を参照。

### D. エッジマーカー (Map view 画面外インジケーター) 機能

- 現在の地図表示領域の「外側(例: 画面端から半径1~2km以内)」にある停留所/駅の方向を、画面の四隅や端にアイコンとして張り付けて表示する。
- ユーザーが「この方向に歩けば乗り物がある」と直感できるようにする。
- 地図のスクロール(パン)に連動して、リアルタイムに位置と表示を更新する。

### E. 日時セレクター

- ユーザーが任意の日時を指定して、過去や未来の乗換案内を探索できる。
- ヘッダーの日時表示をタップすると日時セレクターが開き、カスタム日時を設定可能。
- カスタム日時モード中はヘッダーにインジケーターが表示され、現在時刻に戻すボタンが提供される。
- `?time=` URL パラメータでも初期日時を指定可能 (デモ URL の共有に使用)。
- 日時変更に応じて、出発案内リスト、時刻表、StopMetrics の統計情報が動的に更新される。

### F. Portal / Anchor (お気に入り Stop)

- **Anchor**: ユーザーが明示的に登録するお気に入り Stop。NearbyStop カードのトグルボタンで追加/解除する。
- **Portal**: 登録済み Anchor の一覧ドロップダウン。画面上部に履歴と並んで表示され、選択すると地図がその Stop にパンする。
- **データモデル**: 軽量参照 (stopId, stopName, stopLat, stopLon, routeTypes, createdAt)。GTFS データ更新に強い設計。
- **グルーピング**: `portal?` フィールドで Anchor を名前付きグループに分類可能 (将来拡張)。
- **永続化**: `UserDataRepository` インターフェースを介し、現在は localStorage で永続化。Web API への移行に対応した async + `Result<T>` 設計。

### G. 運行情報 (StopTimes) 機能

- 同じ周辺停留所データに対して、複数の View を提供する。各 View は並び順やグルーピング単位が異なり、ユーザーは目的に応じて表示方法を切り替えられる。

#### 例) `stop` view

- 地図の中心、または現在地から最も近い停留所の情報を優先して表示する。
- **表示件数の制限**: 各路線/行き先(系統)ごとに「直近の最大n便」のみを表示する。
- **時刻のUI表現**:
    - 1便目(次発): 現在時刻から計算し、「あと〇分」という相対時間で強調表示する。
    - 2便目/3便目: 「14:30」などの絶対時刻でシンプルに表示し、比較検討の材料とする。
- 経由地などの情報があれば併記し、行き先の想像を膨らませる工夫を入れる。

#### 例) `route-headsign` view

- 1つの停留所内で、出発案内を `route + headsign` 単位にグルーピングして表示する。
- 同一グループ内では直近の便を時系列順に表示し、ユーザーが同じ行先・同じ系統の次発候補をまとめて比較できるようにする。
- **表示件数の制限**: 各 `route + headsign` グループごとに「直近の最大n便」のみを表示する。
- グループ見出しでは、路線や行先がひと目で区別できることを優先し、必要に応じて route badge や headsign を表示する。
- 個々の便を選択したときは、その `route + headsign` に対応する時刻表詳細へ遷移または展開できること。

### H. 時刻表 (Timetable)

- 停留所または `route-headsign` を起点として、より詳細な時刻表を開けること。
- 時刻表では、出発案内リストより広い時間範囲を一覧でき、便の全体像を把握できること。
- 日時セレクターや関連フィルターの状態と連動し、探索中の日時・条件に対応した内容を表示すること。

### I. 行程 (TripInfo)

- 個々の便に対して、路線、行先、事業者、乗降可否などの行程情報を簡潔に表示すること。
- 出発案内リスト、時刻表、TripInfo 表示など複数の文脈で再利用できる共通表現を持つこと。
- ユーザーが「どの便に乗るか」を比較判断するための最小限の識別情報を常に提供すること。

### J. 選択インタラクション

停留所選択は「どの停留所が選択されているか」という状態であり、複数の方法でトリガーされる。

停留所選択をトリガーする方法 (例):

- 地図上の停留所マーカーをクリック
- 発着案内 (StopBrowser) の停留所をクリック
- 履歴 (StopHistory) から選択
- Anchor / Portal から選択
- URL パラメータ `?stop=` による初期選択

| トリガー                         | 選択状態の変化 | 視覚効果                                                                   |
| -------------------------------- | -------------- | -------------------------------------------------------------------------- |
| 停留所が選択された               | stop 選択 ON   | 選択した停留所のマーカーと、その停留所に関連する路線をハイライト表示する。 |
| 選択中の停留所を再選択           | 変化なし       | -                                                                          |
| 選択を解除 (地図背景クリック 等) | stop 選択解除  | ハイライトを解除し、デフォルト表示に戻す。                                 |

### K. Web Storage による永続化

`localStorage` が使える環境では、以下の情報をセッション間で永続化する。

- **ユーザー設定**: 表示言語、テーマ、tileIndex、infoLevel など
- **Stop 選択履歴**: 直近に選択した Stop の一覧
- **Anchor**: お気に入り Stop の一覧 (F 参照)
- **有効化データソース**: ロード対象として選択中のデータソース prefix の一覧

**Web Storage 不可環境での動作**: ブラウザ設定 (Chrome の「全 Cookie とサイトデータをブロック」等) やプライベートモードで `localStorage` が利用出来ない場合の要件は以下の通り。

- **起動**: `localStorage` getter が例外を投げても crash せず起動する。
- **ユーザー通知**: 起動時に 1 度だけ「永続化されない」旨を toast で通知する。
- **永続化機能**: ストレージへの永続化は行われない。session 内のデータ更新挙動 (in-memory 反映、失敗時の振る舞い) は各機能の実装に委ねる。
- **個別の操作失敗 toast**: 操作の失敗 (アンカー追加、履歴記録など) は通常通り toast で通知する (機能の存在を示すため抑制しない)。
- **UI 表示**: 永続化前提の操作 UI (アンカーボタン、履歴一覧など) は表示を維持する (機能発見性のため非表示にしない)。
- **transient な保存失敗** (`QuotaExceededError` 等) は通常通り通知する。

## 4. システムアーキテクチャ

本プロジェクトは、APIサーバーを持たない完全フロントエンド完結のアーキテクチャを採用する。

- **データパイプライン**: Node.js の事前ビルドパイプライン (`pipeline/`) により、GTFS/ODPT オープンデータから最適化済み JSON を生成し、静的ファイルとして配信する。
- **リポジトリパターン**: UI コンポーネントは `TransitRepository` インターフェースにのみ依存し、データソースと疎結合を保つ。

実装の詳細は [CLAUDE.md](./CLAUDE.md)、[DEVELOPMENT.md](./DEVELOPMENT.md)、[docs/README.md](./docs/README.md) を参照。データパイプラインは独立 subsystem として [pipeline/README.md](./pipeline/README.md) に記載する。

### URL パラメータ

URL パラメータでアプリの動作を制御できる。パラメータは組み合わせて使用可能。

| パラメータ            | 説明                                   | 例                             |
| --------------------- | -------------------------------------- | ------------------------------ |
| `?repo=v2\|mock`      | Repository 実装の切り替え              | `/?repo=v2`                    |
| `?sources=<prefixes>` | 指定した prefix のデータソースのみ有効 | `/?sources=minkuru,yurimo`     |
| `?sources=all`        | disabled を含む全データソースを有効    | `/?sources=all`                |
| `?lat=<number>`       | 初期表示の緯度 (-90 〜 90)             | `/?lat=35.68&lng=139.77`       |
| `?lng=<number>`       | 初期表示の経度 (-180 〜 180)           | `/?lat=35.68&lng=139.77`       |
| `?zm=<number>`        | 初期表示のズームレベル (1 〜 20)       | `/?lat=35.68&lng=139.77&zm=15` |
| `?time=<ISO8601>`     | 初期日時の指定 (RFC 3339)              | `/?time=2026-03-25T20:55`      |
| `?stop=<stop_id>`     | 初期選択 stop の指定                   | `/?stop=minkuru:0442-01`       |
| `?tileIdx=<number>`   | タイルソースの初期選択 (0-based index) | `/?tileIdx=3`                  |
| `?lang=<code>`        | 表示言語の初期選択 (BCP 47)            | `/?lang=en`                    |
| `?bootstrap=hold`     | 起動完了後も Bootstrap 表示を保持      | `/?bootstrap=hold`             |

- `?repo=mock` 指定時は `?sources` は無視される
- `?sources` 指定時は localStorage の設定を上書きするが、localStorage 自体は更新しない (一時的な override)
- パラメータなしの場合、localStorage > `data-source-settings.ts` の `enabled` フィールドの順で決定
- `?lat` と `?lng` は両方指定で有効。片方だけの場合はランダム選択にフォールバック
- `?zm` は単独指定可能 (座標はランダム/env、zoom だけ上書き)
- `?tileIdx` は localStorage の `tileIndex` より優先されるが、localStorage 自体は更新しない (一時的な override)
- `?lang` は localStorage の `lang` より優先されるが、localStorage 自体は更新しない (一時的な override)。`normalizeLang` で正規化される (例: `zh-TW` → `zh-Hant`)。サポート外の値はデフォルト言語にフォールバック
- `?bootstrap=hold` は Bootstrap UI 確認用。repository boot 完了後も Bootstrap 表示を保持し、明示操作で App 表示へ進む
- 無効な値 (非数値、範囲外、Infinity) は無視してフォールバック
- 優先順位: query params > env variables > ランダム選択
- 全パラメータのパースは `src/lib/query-params.ts` で一元管理

## 5. 将来構想

- **GTFS-Flex対応**: 停留所(点)だけでなく、オンデマンド交通の乗降可能エリア(面/ポリゴン)を地図上にオーバーレイ表示し、探索の幅を広げる。
- **PWA化**: オフラインでも次の便を検索できる、完全なお散歩対応アプリへの進化。
- **GTFS-Realtime対応**: リアルタイムの遅延情報を加味した、より正確な出発案内の実現。
