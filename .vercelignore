# Pipeline (scripts, source data, build artifacts)
pipeline/

# Node modules
node_modules

# Dev tooling (not used by the production Vite build)
.github/
.claude/
.storybook/

# Transit data is served from Vercel Blob (rewrite in vercel.json).
public/data-v2/
